log:
  log_dir: "./log"

connection:
  host: "0.0.0.0"
  port: 10000
  max_connection: 10
  max_reception_byte: 100000

msg_converter:
  model: "default"
  encoding: "utf-8"

storage_engine:
  model: "default"
  storage_dir: "./storage/"

visual_model:
  model: "resnet50"
  device: "cuda"